/*
Adam Beighley
CS404
SS 2016

This program assumes the input will be a series of integers seperated by whitespace. A 0 will seperate the series' and the
input file will end on a integer value, not a whitespace. */

#include "ListOfStacks.h"

int main()
{
	ListOfStacks los;
	los.processData();
	return 0;
}